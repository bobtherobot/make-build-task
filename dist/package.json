{
	"name": "make-build-task",
	"displayName": "Make Build Task",
	"description": "Sets up default build task by configuring 'package.json' and 'tasks.json' with a default build task. Allowing for quick setup for building your project with a keyboard shortcut While this sets things up for a javascript environment, ( npm node javascript ), the setup can easily be manually tweaked for others.",
	"version": "0.0.1",
	"publisher": "mike-gieson",
	"repository": "https://github.com/microsoft/vscode-extension-samples/helloworld-sample",
	"engines": {
	  "vscode": "^1.51.0"
	},
	"categories": ["Snippets"],
	"activationEvents": [
		"onCommand:makebuildtask.run"
	],
	"main": "./extension.js",
	"contributes": {
		"commands": [
			{
				"command": "makebuildtask.run",
				"title": "Make Build Task"
			}
		]
	},
	"devDependencies": {
		"@types/vscode": "^1.32.0"
	}
  }
